---
'@syncsoft/tiny-fetch': patch
---

Docs: refresh README badge URLs to bypass stale CDN caches showing "package not found", and align repository / homepage / bugs URLs in `package.json` with the canonical GitHub repo at `Abbosbek-cloud/tiny-fetch`.
